/**
 * Live graph (optional).
 * See docs/ARCHITECTURE.md.
 */

export function LiveGraph() {
  return <div className="h-24 rounded bg-slate-800/50 flex items-center justify-center text-slate-500 text-sm">Live graph (optional)</div>;
}
